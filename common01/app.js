//require('./utils')
const utils = require("./utils");
// const utils = {random,sysInfo}
const { random, info, cc: codecamp } = require("./utils");
console.log(random());
console.log(info());
console.log(codecamp);
